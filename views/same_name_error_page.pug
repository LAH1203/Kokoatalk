doctype html
html
    head
        meta(charset='utf-8' name='viewport' content='width=device-width', initial-scale='1.0')
        title 해당 이름 유저 존재
        style
            include style.css
    body
        div(id='container' class='main_container')
            div(style='padding: 20px;')
            div(class='login_container')
                div(class='form_container')
                    form(name='signup_fail_form' action='/process/signupFail' method='get')
                        div(style='height: 10px;')
                        img(src='/images/logo.jpg' alt='logo title' class='form_logo_title_div' width='300')
                        div(style='height: 80px;')
                        div(class='form_message') 이미 해당 이름의 유저가 존재합니다. 다른 이름으로 회원가입하시겠습니까?
                        div(style='height: 120px;')
                        div
                            button(type='button' class='form_button' onclick="location.href='/signup'") 다른 이름으로 회원가입하기